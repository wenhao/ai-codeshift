<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.wenhao.product.brand.create.mapper.ProductBrandCreateMapper">

    <select id="selectByName" resultType="com.github.wenhao.product.brand.entity.po.ProductBrand">
        SELECT id, name, pic_url AS picUrl, sort, description, status,
               create_time AS createTime, update_time AS updateTime,
               creator, updater, deleted
        FROM product_brand
        WHERE name = #{name} AND deleted = 0
        LIMIT 1
    </select>

</mapper>
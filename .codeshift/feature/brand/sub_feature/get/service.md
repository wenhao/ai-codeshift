## 商品品牌_查询(service)

## service粒度
* 独立的商品品牌_查询service类，ProductBrandGetService。

## 方法描述
查询商品品牌

## 业务逻辑
1. 数据库操作：根据`查询商品品牌-请求参数（Query）-品牌编号`使用`商品品牌(mapper)`的数据库操作对象根据`品牌编号`查询唯一的`商品品牌(entity)`。
2. 数据转换：将数据库查询的`商品品牌_查询(entity)`，转换为`查询商品品牌-响应参数`数据对象。
3. 方法返回：返回`查询商品品牌-响应参数`数据对象。
